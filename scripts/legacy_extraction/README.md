# Legacy Extraction Helpers

These files were moved out of the repository root during Task A hardening.
They are preserved as historical MediaPipe extraction compatibility experiments
and are not part of the Cactus mobile export path.

Several scripts contain container-specific `/app` paths and should be reviewed
before reuse.
